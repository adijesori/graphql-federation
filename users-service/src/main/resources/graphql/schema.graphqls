type User @key(fields: "id") {
    id: ID!
    name: String!
    address: String!
}

type Query {
    users: [User!]!
}

type Order @key(fields: "id") {
    id: ID!
    userId: ID! @external
    user: User @requires(fields: "userId")
}