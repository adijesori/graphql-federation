query UserOrders {
    _entities(representations: [{
        __typename: "User"
        id: "user-1"
    }]) {
        ...on User {
            userOrders {
                date
            }
        }
    }
}