query UserOrders($id: ID!) {
    __entities(representations: [{
        __typename: "User"
        id: $id
    }]) {
        ...on User {
            userOrders {
                date
            }
        }
    }
}